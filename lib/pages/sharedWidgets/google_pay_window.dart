import 'package:flutter/material.dart';

class GooglePlayWindows extends StatelessWidget {
  const GooglePlayWindows({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
